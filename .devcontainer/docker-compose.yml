version: '3.8'

services:
  doc.app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ../..:/home/devlh/workspaces:cached

    # Overrides default command so things don't shut down after the process ends.
    command: sleep infinity
